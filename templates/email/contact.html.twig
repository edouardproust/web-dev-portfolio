<p>
	<b>Date:</b>
	{{ contactMessage.dateTime|date("F jS \\a\\t g:ia") }}</p>
<p>
	<b>Fullname:</b>
	{{ contactMessage.fullName }}</p>
<p>
	<b>Email:</b>
	{{ contactMessage.email }}</p>
<p>
	<b>Subjet:</b>
	{{ contactMessage.subject }}</p>
<p>
	<b>Message:</b>
</p></p>{{ contactMessage.content|nl2br }}</p>
