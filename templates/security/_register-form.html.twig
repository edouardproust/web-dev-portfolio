<h3 class="mb-5">Register for an Account</h3>

{% for type, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ type }}">{{ message|raw }}</div>
    {% endfor %}
{% endfor %}

<p class="mb-5">
    <span>Complete this form to submit you request to become an author on this website.</span>
    <span> Authors can write blog posts and lessons.</span>
    <span> After submitted, your request will be reviewed by the administrator.</span>
    <span> Mandatory fields are mrked with a *.</span>
</p>

{{ form_start(registerForm, {attr: {novalidate: ''}}) }}
{{ form_widget(registerForm) }}
{{ form_errors(registerForm) }}
{{ form_end(registerForm) }}